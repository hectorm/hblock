name: 'Lint'

on:
  push:
    branches: ['*']
  pull_request:
    branches: ['*']

jobs:

  lint:
    name: 'Lint'
    runs-on: 'ubuntu-latest'
    container:
      image: 'docker.io/koalaman/shellcheck-alpine:stable'
    steps:
      - name: 'Install packages'
        run: |
          apk add --no-cache gzip make tar
      - name: 'Checkout project'
        uses: 'actions/checkout@main'
      - name: 'Print ShellCheck version'
        run: |
          shellcheck --version
      - name: 'Lint project'
        run: |
          make clean lint
